meta {
  name: Registrar Usuario
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "operador@en.gestion",
    "password": "operador1234",
    "rol": "OPERADOR"
  }
}
