meta {
  name: Listar Transferencias
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/transferencias
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8080
}
